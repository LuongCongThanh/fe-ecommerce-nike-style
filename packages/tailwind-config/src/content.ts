/** Extra content globs apps should merge in alongside their own `src/**` — covers shared package sources so Tailwind sees classNames used there. */
export const sharedContent = ['../../packages/ui/src/**/*.{ts,tsx}', '../../packages/commerce/src/**/*.{ts,tsx}'];
