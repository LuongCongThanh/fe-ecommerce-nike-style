/** Radius scale (px). Matches the existing shadcn-style --radius base already used in apps/storefront (0.5rem = 8px). */
export const radius = {
  sm: 4,
  md: 6,
  lg: 8,
  xl: 12,
  full: 9999,
} as const;
