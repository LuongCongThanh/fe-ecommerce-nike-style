# Decision Log — Nền tảng E-commerce mới (FE)

> Tách từ [`brainstorm-session.md`](../99-reference/planning/brainstorm-session.md) khi phần decision log trở nên quá dài để nằm chung với narrative. File này chỉ giữ hai thứ: quyết định đã chốt và lý do chọn. Nếu cần bối cảnh dẫn tới quyết định, xem lại `brainstorm-session.md`.

## Cách dùng tài liệu này

- Tìm ở đây khi cần biết một quyết định đã được chốt hay chưa.
- Nếu một quyết định đã có ở đây, các tài liệu phía dưới không được tự ý đi ngược lại.
- Nếu cần bối cảnh, giả định hoặc tranh luận dẫn tới quyết định, quay lại [`brainstorm-session.md`](../99-reference/planning/brainstorm-session.md).

## Lối vào nhanh

- Nếu bạn đang hỏi về scope nền tảng ban đầu: xem Decision `#1–#11`.
- Nếu bạn đang hỏi về i18n, locale, typography: xem Decision `#14–#19`.
- Nếu bạn đang hỏi về auth, token, state, testing, performance: xem Decision `#20–#28`.
- Nếu bạn đang hỏi về cấu trúc code và quy ước repo: xem Decision `#29–#34`, `#46–#49`.
- Nếu bạn đang hỏi về domain commerce như cart, wishlist, order, return: xem Decision `#35–#45`.

## Cụm quyết định chính

- `#1–#11`: hướng đi sản phẩm, mock-first, foundation-first, và vai trò của tài liệu tham khảo.
- `#12–#13`, `#29–#34`, `#46–#49`: kiến trúc monorepo, package boundary, naming, import rule, và quy ước scaffold.
- `#14–#19`: domain locale/i18n.
- `#20–#28`: auth, design token, state, API mocking, testing, performance.
- `#35–#45`: domain model commerce cốt lõi.
- `#50`: mock catalog data spec (quy mô, không phải domain model — domain model catalog đã chốt ở `glossary.md`).
- `#51–#54`: developer-experience tooling (ESLint plugin set, git hooks/commit lint, Zustand devtools, bundle analyzer).
- `#55–#56`: mở rộng naming convention và nâng mức ESLint TypeScript ruleset.
- `#57`: bỏ barrel `index.ts` (thay thế phần barrel của #29), export package qua `package.json` `"exports"` subpath, alias-only import.
- `#58`: PWA (`next-pwa`), production-only để tránh xung đột service worker với MSW.
- `#59`: 3D visualization (Three.js) chỉ ở storefront PDP.

## Nhật ký quyết định (Decision Log)

| # | Quyết định | Phương án khác đã cân nhắc | Lý do chọn |
|---|---|---|---|
| 1 | Dự án FE mới, độc lập với `ecommerce-next` | Cải tiến kiến trúc `ecommerce-next` hiện có; chỉ làm nơi lưu tài liệu | Muốn một sản phẩm/dự án riêng |
| 2 | Sản phẩm thật, định hướng kinh doanh | Học tập/portfolio | Mục tiêu là kinh doanh thật |
| 3 | Mock-first (MSW + localStorage), backend chưa chọn | Dùng lại Django có sẵn; dùng NestJS (`be-nest-ecom`) | Backend thật chưa cần chốt ngay |
| 4 | Turborepo monorepo ngay từ Phase 1 | Single Next.js app trước, tách monorepo sau | Chấp nhận overhead ban đầu để dễ mở rộng dài hạn |
| 5 | MVP scaffold cả 3 app: storefront + admin + cms | Chỉ storefront; storefront + admin | Muốn đủ 3 mảng ngay từ đầu |
| 6 | CMS **tự xây** | Headless CMS bên thứ 3 (Sanity/Strapi) | Đã cân nhắc trade-off, vẫn chọn tự xây để kiểm soát hoàn toàn |
| 7 | Thanh toán MVP: chỉ COD | Mock luôn cả cổng thanh toán online | Đơn giản hoá giai đoạn mock-first |
| 8 | Đa ngôn ngữ VN + EN từ đầu | Chỉ tiếng Việt | Không muốn retrofit i18n sau |
| 9 | Không áp lực deadline cụ thể | 1–2 tháng / 3–6 tháng | Solo dev, ưu tiên chất lượng |
| 10 | Chọn **Hướng B — Foundation-first**: xây core components, core functions, design system đầy đủ trước feature | Hướng A (scaffold nhẹ, đào sâu từng module trước, tách package theo rule-of-three) | Muốn nền tảng component/design-system vững trước, chấp nhận rủi ro over-engineering đã cảnh báo |
| 11 | `implementation-plan.md` là tài liệu **tham khảo thêm** (giống `vision-sketch.md`, `nike-ui-ux-analysis.md`), không thay thế thiết kế đang làm | Dùng implementation-plan.md làm tài liệu gốc thay cho thiết kế hiện tại | Tiếp tục thiết kế từng phần như đang làm, đối chiếu để không mâu thuẫn |
| 12 | Không tách `layouts` thành package riêng — primitive bố cục (`Container`, `Grid`, `Stack`, `Section`) nằm trong `packages/ui`; `Header`/`Footer` cụ thể nằm trong từng `apps/*` | Tách `packages/layouts` riêng | Header/Footer khác nhau nhiều giữa storefront/admin/cms; `implementation-plan.md` cũng không tách package `layouts` riêng — nhất quán |
| 13 | Thêm `packages/schemas` (Zod schemas — request/response/error contract) vào cấu trúc monorepo | Không có package riêng, để schema rải rác trong `api-sdk` | Theo nguyên tắc Contract-first của `implementation-plan.md`: schema phải định nghĩa trước khi build UI/mock — cần một nơi tập trung, dùng chung giữa `api-sdk`, `commerce`, và form validation |
| 14 | Tách `Locale` (ngôn ngữ hiển thị) và `Market` (vùng kinh doanh: tiền tệ/catalog/thuế) thành 2 khái niệm domain riêng biệt — hiện chỉ dùng `Locale`, chưa cần `Market` | Gộp chung "đa ngôn ngữ" = đổi cả tiền tệ/catalog | Tránh nhầm lẫn domain; xem `glossary.md` |
| 15 | Danh sách Locale đóng, một nguồn sự thật duy nhất trong code (`SUPPORTED_LOCALES`); thêm locale = sửa code + deploy | Danh sách locale runtime/CMS-managed (Admin bật/tắt không cần deploy) | Tránh lặp lỗi hard-code rải rác đã thấy ở `ecommerce-next` (`middleware.ts`); chưa có nhu cầu quản lý locale động — xem **ADR 0001** |
| 16 | Locale áp dụng cho cả UI chrome lẫn nội dung Product/CMS (`Localized Text`); thiếu bản dịch thì fallback về Locale mặc định (`vi`), không chặn publish, không hiển thị trống | Bắt buộc dịch đủ mới publish; hiển thị trống khi thiếu bản dịch | Không có đội dịch thuật riêng, không nên khoá vận hành vì thiếu bản dịch — xem `glossary.md` (`Locale mặc định`, `Localized Text`) |
| 17 | Đa Locale (UI) chỉ áp dụng cho `storefront`; `admin`/`cms` chỉ tiếng Việt (Content Editor vẫn nhập Localized Text đa ngôn ngữ cho Product/CMS content) | Đa Locale cho cả 3 app | Người vận hành nội bộ, giả định biết tiếng Việt; dịch UI Admin/CMS không phục vụ khách hàng cuối — xem **ADR 0002** |
| 18 | Routing dùng lại pattern `app/[locale]/...` (`next-intl`) cho `storefront`, như `ecommerce-next` đã kiểm chứng; `admin`/`cms` không có segment `[locale]` | Tự thiết kế routing pattern khác | Pattern đã chạy production-like, nhất quán với Decision #17 |
| 19 | Typography token: **một bộ duy nhất** dùng chung mọi Locale, line-height tính dư khoảng trống cho dấu tiếng Việt; không override riêng theo locale | Typography token override riêng theo locale (`en` chặt theo Nike gốc, `vi` nới hơn) | `storefront` đổi locale ngay trong phiên → override riêng gây layout shift khi đổi ngôn ngữ — xem **ADR 0003**. Ràng buộc kèm theo: font phải có đầy đủ glyph tiếng Việt |
| 20 | Authentication **có** trong MVP storefront (sign in/up, account, order history, wishlist gắn user) | Guest-only cho MVP, auth để P1 | Xác nhận trực tiếp khi được hỏi trong phiên thiết kế solution — xem [`FE/FE-ARCHITECTURE.md`](../FE/FE-ARCHITECTURE.md) §11. `admin`/`cms` vốn đã cần auth nội bộ bất kể lựa chọn này |
| 21 | Phạm vi "tự xây CMS" Phase 1 = **toàn bộ CMS MVP** như `implementation-plan.md` liệt kê (Hero Banner, Homepage Sections, Collection Landing Page, Promotion Banner, SEO Metadata, Blog, Campaign) | Lát cắt mỏng — chỉ Hero/Banner trước | Xác nhận trực tiếp khi được hỏi trong phiên thiết kế solution |
| 22 | Cơ chế authentication ban đầu: session cookie `httpOnly` tự quản lý, không dùng NextAuth/Auth.js hay provider hosted (Clerk...). **Đã bị thay thế bởi Decision #65.** | NextAuth/Auth.js; hosted provider (Clerk, Supabase Auth) | Quyết định lịch sử trước khi Backend thật được chốt; xem ADR 0004 và ADR 0010 |
| 23 | Cơ chế hiện thực hoá design token: **Tailwind CSS v4 (`@theme`)** + CSS custom properties sinh từ `packages/design-tokens` | CSS-in-JS (vanilla-extract, styled-components) | Khớp với `packages/ui` (dùng Radix + shadcn-style component, vốn build trên Tailwind); tránh runtime CSS-in-JS overhead ảnh hưởng mục tiêu Lighthouse > 95 — xem [`FE/FE-ARCHITECTURE.md`](../FE/FE-ARCHITECTURE.md) §16 |
| 24 | State client dùng chung (không phải server state/URL state): **Zustand** | Redux Toolkit; React Context + `useReducer`; Jotai | Ít boilerplate nhất cho phạm vi còn lại sau khi tách URL-state và TanStack Query; selector tránh re-render thừa mà Context không có mặc định — xem [`FE/FE-ARCHITECTURE.md`](../FE/FE-ARCHITECTURE.md) §13 |
| 25 | Visual regression testing: **Playwright screenshot comparison** (`toHaveScreenshot()`) | Chromatic; Percy | Đã có sẵn Playwright cho E2E, không thêm công cụ/chi phí mới; chưa cần dashboard review cộng tác cho solo dev — xem [`FE/FE.md`](../FE/FE.md) §4.6 |
| 26 | Font-loading cho `storefront`: self-host, subset theo bộ ký tự Latin + dấu tiếng Việt, preload weight above-the-fold | Load runtime từ Google Fonts | Tránh round-trip domain thứ ba, kiểm soát cache-control — xem [`FE/FE.md`](../FE/FE.md) §9. Còn mở: `font-display: swap` vs `optional` — cần đo CLS thực tế ở size Heading/Display (48–72px) trước khi chốt giá trị cuối |
| 27 | Performance budget cho `admin`/`cms`: LCP < 4s, INP < 500ms, không có mục tiêu Lighthouse tổng thể | Áp cùng budget nghiêm ngặt như `storefront` (LCP < 2.5s, Lighthouse > 95) | App nội bộ, không traffic công khai/SEO — budget nghiêm ngặt là over-engineering không tương xứng; xem [`FE/FE.md`](../FE/FE.md) §9 |
| 28 | Cơ chế chuyển `packages/api-sdk` từ MSW mock sang API thật: biến môi trường (`NEXT_PUBLIC_API_MOCKING`) đọc ở entrypoint khởi tạo, hàm fetch giữ nguyên chữ ký ở cả hai chế độ | Tách riêng 2 implementation không qua flag; branch theo `NODE_ENV` | Đơn giản, không cần rewrite component khi chuyển — xem [`FE/FE-ARCHITECTURE.md`](../FE/FE-ARCHITECTURE.md) §6.6. Chi tiết cụ thể (tên biến, nơi đọc) có thể tinh chỉnh khi thật sự cần ở Phase 3 |
| 29 | Cấu trúc bên trong mỗi `apps/*`: mô hình feature-module — `features/{feature}/pages/{page}/` dạng thư mục (page + component/hook riêng trang), route Next.js chỉ mount page component (áp dụng skill `frontend-architecture`, portable module-based). **Phần "barrel `index.ts` là điểm import duy nhất" đã bị thay thế bởi Decision #57** (không dùng barrel, public API định nghĩa theo path) | Feature folder phẳng không phân trang/module rõ ràng; đặt page trực tiếp trong `app/` không tách logic | Cấu trúc tự mô tả được ("code này ở đâu, ai được import gì" trả lời được không cần hỏi), tương thích ESLint boundaries đã có — xem [`FE/FE-ARCHITECTURE.md`](../FE/FE-ARCHITECTURE.md) §7 |
| 30 | Quy ước đặt tên: interface tiền tố `I`, PascalCase component, kebab-case thư mục trang, `use`-prefix hook, store `{feature}.store.ts` | Không có quy ước thống nhất, để tự do theo từng người viết | Nhất quán khi scaffold code thật, tránh mỗi package/feature một kiểu — xem [`FE/FE-ARCHITECTURE.md`](../FE/FE-ARCHITECTURE.md) §7.4 |
| 31 | Query key factory phân cấp cho TanStack Query, đặt trong `packages/hooks` (hoặc `features/{feature}/hooks` nếu riêng 1 feature) | String key rời rạc, tự nhớ thủ công từng key khi invalidate | Invalidate cache nhất quán theo entity (list/detail) — xem [`FE/FE-ARCHITECTURE.md`](../FE/FE-ARCHITECTURE.md) §6.7 |
| 32 | Zustand: 1 store unit/feature (`features/{feature}/stores/{feature}.store.ts`), interface tiền tố `I`, luôn có `reset()`; nguyên tắc chung: không copy server state (TanStack Query) vào Zustand | Nhiều store rời rạc không theo feature; copy response server vào Zustand để "cache lại" | Nhất quán với Decision #24 và quy ước đặt tên #30; tránh 2 nguồn sự thật cho cùng dữ liệu — xem [`FE/FE-ARCHITECTURE.md`](../FE/FE-ARCHITECTURE.md) §13 |
| 33 | Tổ chức class Tailwind trong component: **inline trong JSX + `cva` cho variant** làm mặc định; chỉ tách `{component}.styles.ts` khi component đủ phức tạp (rule-of-three) | Luôn co-locate mọi class ra file `{component}.styles.ts` riêng (mặc định của skill `frontend-architecture`) | Giữ lợi ích cốt lõi của Tailwind (markup + style cùng chỗ); nhất quán YAGNI/Foundation-first đã chọn (Decision #10) thay vì áp quy ước tách file cho toàn bộ design system ngay từ đầu — xem [`FE/FE-ARCHITECTURE.md`](../FE/FE-ARCHITECTURE.md) §16 |
| 34 | Tách `packages/tailwind-config` làm preset Tailwind v4 dùng chung cho cả 3 `apps/*` | Mỗi app tự import CSS custom properties từ `design-tokens`, không có package riêng | Nhất quán với cách `eslint-config`/`ts-config` đã tách — mọi shared config đều là package riêng, không có ngoại lệ; xác nhận trực tiếp khi được hỏi trong phiên grilling đánh giá cây thư mục — xem [`FE/FE-ARCHITECTURE.md`](../FE/FE-ARCHITECTURE.md) §6.2 |
| 35 | `CartItem` tham chiếu thẳng SKU (`{ skuId, quantity }`), không lưu `productId` + Color/Size rời | Lưu `productId` + Color/Size, tra ngược ra SKU mỗi lần cần giá/tồn kho | Khớp đúng định nghĩa SKU đã chốt (giá/tồn kho gắn ở SKU) — tránh logic tra cứu lặp lại nhiều nơi; xem `glossary.md` |
| 36 | Merge guest cart vào user cart: **cộng dồn** quantity khi trùng SKU, sau đó clamp theo `available` | Lấy giá trị lớn hơn giữa hai bên (không cộng dồn) | Nhất quán hành vi "thêm vào giỏ" thông thường; clamp để không vượt tồn kho — xem `glossary.md` |
| 37 | Reservation tồn kho chỉ tạo khi bắt đầu Checkout, không tạo khi add-to-cart; có expiry | Reserve ngay khi add-to-cart | Tránh khoá tồn kho oan khi khách add vào giỏ rồi bỏ đi cả buổi không checkout — xem `glossary.md` |
| 38 | Reservation → committed stock ngay khi Order tạo thành công (Place Order COD thành công) | Giữ dạng Reservation cho tới khi DELIVERED | Luồng COD không có bước payment gateway/webhook để chờ xác nhận — Place Order thành công là thời điểm đáng tin duy nhất; huỷ sau đó xử lý bằng nhánh CANCELLED/RETURN riêng, không phải reservation expiry — xem `glossary.md` |
| 39 | Order chỉ CANCELLED được từ `PENDING`/`PROCESSING`; từ `PACKED` trở đi phải qua `RETURN_REQUESTED` → `RETURNED` sau khi `DELIVERED`, không huỷ thẳng | Cho phép CANCELLED từ bất kỳ trạng thái nào trước DELIVERED | Sau khi đã đóng gói vật lý, huỷ đơn không còn là thao tác đơn giản — cần đúng quy trình dừng giao/nhận hàng hoàn, không phải chuyển trạng thái tức thời — xem `glossary.md` |
| 40 | `WishlistItem` tham chiếu Product, không gắn Variant/SKU cụ thể | Gắn SKU cụ thể giống CartItem | Khớp UX bấm tim ngay trên ProductCard ở PLP, nơi chưa chọn Color/Size — xem `glossary.md` |
| 41 | Merge guest wishlist vào user wishlist: hợp nhất (union) theo Product, khử trùng | (Không có phương án khác — không có quantity nên không có xung đột cần chọn) | Nhất quán hệ quả trực tiếp của Decision #40 (WishlistItem không có quantity) — xem `glossary.md` |
| 42 | "Move to cart" từ Wishlist: Product có Variant → điều hướng PDP để tự chọn; Product không Variant → add thẳng vào Cart | Luôn add thẳng vào cart, tự chọn Variant đầu tiên còn hàng | Hệ thống không thể tự suy đoán Color/Size khách muốn khi WishlistItem không gắn SKU (Decision #40) — xem `glossary.md` |
| 43 | Cửa sổ trả hàng (Return window): 7 ngày kể từ Order chuyển DELIVERED, quá hạn hệ thống từ chối tạo RETURN_REQUESTED | 15 ngày; 30 ngày; không giới hạn, để nhân viên tự xem xét | Cần một rule cứng để hệ thống tự enforce, không phải judgment call thủ công mỗi lần; 7 ngày phù hợp fashion/giày dép tại VN — xem `glossary.md` |
| 44 | RETURN_REQUESTED cần bước duyệt thủ công (nhân viên kiểm tra hàng trả về) trước khi thành RETURNED; có thể bị từ chối, đơn quay lại DELIVERED | Mọi RETURN_REQUESTED tự động thành RETURNED, không có bước duyệt | Cần xác nhận hàng trả về thực tế đủ điều kiện (chưa qua sử dụng, đủ phụ kiện) trước khi giải phóng tồn kho và ghi nhận hoàn tiền — xem `glossary.md` |
| 45 | Hoàn tiền COD: hệ thống chỉ ghi nhận số tiền cần hoàn + thông tin nhận hoàn, không tự động chuyển tiền; nhân viên tự chuyển khoản tay rồi đánh dấu đã hoàn | Không hỗ trợ hoàn tiền, chỉ cho đổi hàng (exchange) | MVP không có cổng thanh toán online để tích hợp refund API tự động — phù hợp quy mô solo dev/COD-only — xem `glossary.md` |
| 46 | Chốt 3 mục [Đề xuất] còn lại trong `FE-ARCHITECTURE.md` giữ nguyên hiện trạng: (a) quy tắc phụ thuộc một chiều giữa các package, (b) quy ước TypeScript (`strict: true`, cấm `any`, `import type` bắt buộc, return type tường minh cho hàm public), (c) bảng alias import (`@/*`, `@/features/{feature}`, `@repo/*`) | Điều chỉnh một phần trước khi chốt (nới lỏng strict, đổi quy tắc phụ thuộc, đổi tên alias) | Xác nhận trực tiếp trước khi scaffold Phase 0 — cả 3 sẽ bị khoá vào ESLint config + tsconfig gốc ngay từ đầu, đổi sau tốn công refactor toàn repo; không mâu thuẫn quyết định nào đã chốt trước đó (#10, #12, #13) — xem [`FE/FE-ARCHITECTURE.md`](../FE/FE-ARCHITECTURE.md) §7–§8 |
| 47 | Không bắt buộc `README.md` ở cấp module (`features/{feature}/`) và cấp page (`pages/{page}/`) — khác mặc định của skill `frontend-architecture` | Bắt buộc cả 2 cấp như skill gốc; chỉ bắt buộc cấp module | Ở quy mô solo dev, README cấp page trôi lỗi thời nhanh vì page nhỏ, thay đổi liên tục — chi phí duy trì vượt giá trị đọc lại; bỏ luôn cấp module để nhất quán, tránh tài liệu song song dễ lệch với code khi cấu trúc đã tự mô tả qua barrel + quy ước đặt tên (Decision #29/#30) — xem [`FE/FE-ARCHITECTURE.md`](../FE/FE-ARCHITECTURE.md) §7 |
| 48 | Không tách thư mục `constants/` riêng ở cấp page hay cấp feature — hằng số chỉ 1 nơi dùng khai báo ngay trong file dùng nó; chỉ tách file đơn `features/{feature}/constants.ts` khi ≥2 nơi trong cùng feature cần dùng chung | Luôn có sẵn `constants/` ở cả page và feature như skill `frontend-architecture`, dù trống | Nhất quán rule-of-three/YAGNI đã áp dụng xuyên suốt (Decision #10, xem [`FE/FE-ARCHITECTURE.md`](../FE/FE-ARCHITECTURE.md) §16.2 "component alias") — không tạo thư mục trước khi có nhu cầu thật — xem [`FE/FE-ARCHITECTURE.md`](../FE/FE-ARCHITECTURE.md) §7 |
| 49 | Không có `services/` riêng cho từng feature — mọi network call đi qua `packages/api-sdk` dùng chung (Zod-typed, MSW mock), dù chỉ 1 feature tiêu thụ | Thêm `features/{feature}/services/` riêng cho data-access chỉ dùng trong 1 feature, tách biệt `packages/api-sdk` dùng chung nhiều app | Nhất quán Decision #13 (Contract-first qua `packages/schemas`/`packages/api-sdk`); tránh 2 nơi gọi API khi feature đó sau này cần share cross-app — `api-sdk` không phân biệt "riêng 1 feature" hay "dùng chung", mọi typed fetch function đều bình đẳng — xem [`FE/FE-ARCHITECTURE.md`](../FE/FE-ARCHITECTURE.md) §2 (ARC-004) |
| 50 | Mock catalog spec cho scaffold/Phase 3: 3 Category cấp 1 (`Shoes`, `Apparel`, `Accessories`) × 2 category con mỗi nhóm (VD `Shoes > Running/Basketball`, `Apparel > T-Shirts/Hoodies`, `Accessories > Bags/Hats`); ~24–30 Product tổng, trải đều 6 category con; trộn Product có Variant (Color × Size) và Product không Variant; Gender filter đủ 4 giá trị (men/women/kids/unisex); nội dung/ảnh placeholder, không dùng data Nike thật | Chờ catalog thật/SKU thật từ nguồn hàng thật trước khi build FE catalog/PDP/cart | Catalog thật phụ thuộc business chưa có nguồn hàng — không nên chặn Phase 3; quy mô đủ để test pagination (grid 2/3/4 cột đã chốt ở [`FE/FE-ARCHITECTURE.md`](../FE/FE-ARCHITECTURE.md) §16.4.4) và đủ đa dạng để cover case Product không Variant (đã ghi trong `glossary.md`); nội dung sẽ nằm trong MSW mock handler ở `packages/api-sdk/mocks`, không hardcode trong UI component — xem [`FE/FE-EXECUTION.md`](../FE/FE-EXECUTION.md) §2.7 |
| 51 | Bộ ESLint plugin cho `packages/eslint-config`: `typescript-eslint` (TS rules), `eslint-plugin-import-x` (import order), `eslint-plugin-boundaries` (boundaries), `eslint-config-next` (Next.js rules), `eslint-plugin-react` + `eslint-plugin-react-hooks` (React rules), `eslint-plugin-jsx-a11y` (a11y rules) | Tự viết rule thủ công không qua plugin có sẵn; trì hoãn chọn plugin tới khi scaffold thật | Các rule (import order, boundaries, TS strict, a11y) đã được ghi ý định ở nhiều nơi (`FE-EXECUTION.md` §1.6.2, Decision #46, `FE-ARCHITECTURE.md` DS-005) nhưng chưa gắn tên package cụ thể — chặn ngay bước 2 của Phase 0 nếu không chốt trước; `eslint-plugin-jsx-a11y` thêm mới để tự động hoá DS-005 (a11y baseline) thay vì chỉ review thủ công — xem [`FE/FE-EXECUTION.md`](../FE/FE-EXECUTION.md) §1.6.2, §2.7, §3.1 |
| 52 | Git hooks + commit lint chốt dùng ngay từ Phase 0 (không còn optional): `husky` (`pre-commit` chạy `lint-staged`, `commit-msg` chạy `commitlint`), `lint-staged` (eslint --fix + prettier --write file staged), `@commitlint/cli` + `@commitlint/config-conventional` (Conventional Commits) | Giữ như "Optional nhưng hữu ích" (`FE.md` §5.6 cũ), tự nhớ chạy lint/format thủ công trước mỗi commit | Chi phí thấp, giá trị rõ ngay (chặn commit code lỗi format/lint, ép convention message dễ đọc lại lịch sử) — solo dev vẫn có giá trị vì tự động hoá thay tự nhớ; không thêm `commitizen` (interactive prompt) vì chỉ 1 người viết commit, không cần UI hỏi từng bước — xem [`FE/FE-EXECUTION.md`](../FE/FE-EXECUTION.md) §2.16 |
| 53 | Zustand devtools middleware (có sẵn trong package `zustand`, không cài thêm) bọc mọi store `{feature}.store.ts`, chỉ bật khi `NODE_ENV === "development"` | Không dùng devtools, debug qua `console.log` thủ công trong store | Xem lịch sử thay đổi state qua Redux DevTools extension nhanh hơn log thủ công; tắt ở production tránh leak state qua devtools — xem [`FE/FE-ARCHITECTURE.md`](../FE/FE-ARCHITECTURE.md) §13.1 |
| 54 | `@next/bundle-analyzer` chốt dùng ở `apps/*`, chạy qua biến `ANALYZE=true` khi cần, không chạy mặc định mỗi lần build | Không theo dõi bundle size ở Phase 0, chỉ đo khi Lighthouse báo vấn đề | Mục tiêu Lighthouse > 95 (`FE.md` §9) cần công cụ soi bundle sớm hơn là phát hiện muộn qua Lighthouse; chi phí thêm gần như 0 (chỉ chạy khi gọi `ANALYZE=true`, không ảnh hưởng build thường) — xem [`FE/FE-EXECUTION.md`](../FE/FE-EXECUTION.md) §2.17 |
| 55 | Mở rộng naming convention (bổ sung Decision #30): biến/hàm `camelCase`; constant module-level `SCREAMING_SNAKE_CASE`; `type` alias `PascalCase` không tiền tố (khác `interface` có tiền tố `I`); `interface` cho object shape có thể extend, `type` cho union/intersection/primitive alias; boolean tiền tố `is`/`has`/`should`; event handler prop tiền tố `on`, handler nội bộ tiền tố `handle`; không dùng TypeScript `enum` (dùng union string literal hoặc `as const` object) | Để ngỏ, mỗi người tự chọn theo convention JS/TS phổ biến; hoặc cho phép `enum` như một phần TypeScript chuẩn | Decision #30 chỉ chốt component/hook/interface/store/page-dir, chưa đủ để tránh mỗi phần code viết theo style khác nhau; cấm `enum` khớp khuyến nghị TypeScript hiện đại (tránh runtime overhead, tránh vấn đề tree-shaking/erasableSyntaxOnly) — xem [`FE/FE-ARCHITECTURE.md`](../FE/FE-ARCHITECTURE.md) §7.4 |
| 56 | Nâng ESLint TypeScript ruleset từ `tseslint.configs.recommended` lên `strictTypeChecked` + `stylisticTypeChecked` (type-aware, cần `parserOptions.project`) | Giữ `recommended` (không cần type info, lint nhanh hơn) | Khớp đúng mức "TypeScript strict" đã chốt (Decision #46) — `recommended` không bắt được lỗi cần type info như floating promise, unnecessary type assertion; đánh đổi lint chậm hơn chấp nhận được ở quy mô hiện tại — xem [`FE/FE-EXECUTION.md`](../FE/FE-EXECUTION.md) §2.7 |
| 57 | **Thay thế phần barrel của Decision #29**: bỏ hoàn toàn `index.ts` barrel — cả cấp feature (`features/{feature}/index.ts`) và cấp package (`packages/*/src/index.ts`). Feature: public API định nghĩa theo path (`pages/**` là public, `components/`/`hooks/`/`stores/`/`utils/**` private), enforce bằng `eslint-plugin-boundaries`. Package: export qua `package.json` `"exports"` subpath map (VD `@repo/ui/button`) thay vì 1 barrel tổng. Mọi import bắt buộc dùng alias (`@/*`, `@repo/*`), cấm import tương đối leo cấp cha (`../`, rule `import-x/no-relative-parent-imports`) | Giữ barrel `index.ts` như Decision #29 gốc | Xác nhận trực tiếp khi được hỏi — barrel file lớn là antipattern hiệu năng đã biết (dev-mode bundler chậm khi barrel gom nhiều export, đặc biệt với Next.js/Turbopack); path-based boundary + subpath export vẫn giữ được mục tiêu gốc của Decision #29 (ranh giới public/private rõ, dev không tự chia mỗi feature một kiểu) mà không cần file barrel; alias-only import loại bỏ luôn việc phải sửa hàng loạt path tương đối khi di chuyển file — xem [`FE/FE-ARCHITECTURE.md`](../FE/FE-ARCHITECTURE.md) §6.9, §7.1–§7.4, §8; [`FE/FE-EXECUTION.md`](../FE/FE-EXECUTION.md) §1.6, §2.7 |
| 58 | PWA dùng `@ducanh2912/next-pwa`, chỉ ở `storefront`, service worker **chỉ bật ở production build** (`disable` khi `NODE_ENV === "development"`) | Bật PWA service worker luôn cả dev; dùng cho cả 3 app; không thêm PWA | MSW (Decision #28) đã đăng ký service worker riêng cho browser-mode mock ở dev — 2 service worker không thể cùng kiểm soát 1 scope; giới hạn PWA chỉ production tránh xung đột hoàn toàn vì lúc đó MSW browser-mode không còn active; `admin`/`cms` là app nội bộ không cần installable — xem [`FE/FE-ARCHITECTURE.md`](../FE/FE-ARCHITECTURE.md) §4.1.2 |
| 59 | 3D visualization dùng `three` + `@react-three/fiber` + `@react-three/drei`, chỉ ở `storefront` PDP (product viewer 3D xoay 360°, đồng bộ màu theo Variant đang chọn ở SKU selector) — không dùng cho Hero Banner/Homepage/Collection landing hay `admin`/`cms`; bắt buộc lazy-load qua `next/dynamic({ ssr: false })` chỉ khi vào route PDP | Dùng ở Homepage/Hero Banner cho hiệu ứng thị giác; dùng raw `three` không qua `@react-three/fiber`; mở rộng sang AR try-on | Rà soát các vị trí khả dĩ (PDP, Homepage/Hero, Collection landing, Admin/CMS asset preview, AR try-on) — chỉ PDP vừa có giá trị UX thật (xem SKU/Variant trực quan) vừa không đụng mục tiêu Lighthouse > 95 đã chốt ở `storefront` (Homepage/Hero/Collection đều SEO-critical, rủi ro trực tiếp nếu thêm WebGL); `@react-three/fiber` khớp codebase React hiện tại hơn imperative three.js thuần; AR/Admin preview không có yêu cầu nào trong `functional-requirements.md`, thêm vào là vi phạm YAGNI đã áp dụng xuyên suốt — xem [`FE/FE-ARCHITECTURE.md`](../FE/FE-ARCHITECTURE.md) §4.1.1, [`FE/FE.md`](../FE/FE.md) §4.7 |
| 60 | Backend dùng Node.js LTS + TypeScript strict + NestJS theo kiến trúc modular monolith | Django REST; Express/Fastify thuần; microservices | Solo developer có 6 năm kinh nghiệm FE/TypeScript nhưng chưa làm backend: giữ cùng ngôn ngữ giảm số hệ sinh thái phải học; NestJS cung cấp module, dependency injection, guard, validation và OpenAPI theo convention; modular monolith giữ ranh giới domain mà không thêm chi phí vận hành microservices. Database, ORM và hạ tầng được grill riêng — xem [`adr/0005-nestjs-modular-monolith-backend.md`](adr/0005-nestjs-modular-monolith-backend.md) |
| 61 | PostgreSQL là primary database của Backend | MySQL; MongoDB; SQLite | Domain commerce có nhiều quan hệ và invariant transaction giữa SKU, Reservation, Inventory, Order và OrderItem; PostgreSQL cung cấp foreign key, unique constraint, transaction isolation và locking phù hợp để chống oversell. MongoDB không tạo lợi thế cho mô hình quan hệ/transaction-heavy, SQLite không phù hợp concurrency production, còn MySQL không có lợi thế dự án cụ thể đủ để đổi hướng — xem [`adr/0006-postgresql-primary-database.md`](adr/0006-postgresql-primary-database.md) |
| 62 | Prisma là ORM/data-access mặc định; raw SQL chỉ dùng có kiểm soát khi Prisma không biểu đạt đủ transaction, locking hoặc query đặc thù | Drizzle; TypeORM; raw SQL cho toàn bộ persistence | Prisma cho type-safe query, schema, migration và tooling dễ tiếp cận với developer mới làm Backend; transaction API đáp ứng phần lớn use case. Escape hatch raw SQL giữ quyền kiểm soát ở các critical path như inventory/checkout mà không buộc toàn dự án viết SQL thủ công. Không tạo generic repository abstraction trước khi có nhu cầu thật — xem [`adr/0007-prisma-default-data-access.md`](adr/0007-prisma-default-data-access.md) |
| 63 | Backend nằm trong repository riêng, độc lập với FE Turborepo về source, lifecycle và deployment; đây vẫn là một modular monolith, không phải microservices | Đặt NestJS ở `apps/api` trong FE Turborepo | Chủ dự án muốn FE và BE là hai dự án độc lập. Đánh đổi được chấp nhận: không import trực tiếp `packages/schemas`, thay đổi contract không còn atomic trong một commit, cần versioned API contract/OpenAPI và quy trình compatibility giữa hai repo — xem [`adr/0008-separate-backend-repository.md`](adr/0008-separate-backend-repository.md) |
| 64 | Sau API v1 handshake, versioned OpenAPI artifact do BE phát hành là canonical transport contract; FE pin phiên bản và generate client/Zod adapters. `packages/schemas` hiện tại là baseline chuyển tiếp để thiết kế API v1 | Chia sẻ source/package Zod trực tiếp giữa hai repo; duy trì DTO/Zod thủ công ở cả hai bên; tạo contract repo thứ ba | Hai repo đã chốt độc lập ở Decision #63 nên cần giao diện máy đọc được, có version và kiểm tra compatibility. NestJS sinh OpenAPI; FE generate consumer artifacts để tránh copy type bằng tay. Breaking change phải phát hành contract version mới, không để FE tự lấy `latest` — xem [`adr/0009-versioned-openapi-contract.md`](adr/0009-versioned-openapi-contract.md) |
| 65 | **Thay thế Decision #22**: browser auth dùng JWT access token ngắn hạn giữ trong memory; refresh token là opaque random token trong cookie `HttpOnly`/`Secure`/`SameSite`, rotate mỗi lần dùng và lưu hash/family trong PostgreSQL để revoke/reuse detection. Không lưu token trong `localStorage`/`sessionStorage` | Opaque server-side session cookie; JWT dài hạn trong browser storage; JWT cho cả access và refresh token | Chủ dự án chọn JWT. Mô hình access JWT + rotating opaque refresh token giữ được JWT cho API nhưng vẫn hỗ trợ revoke/logout/reset-password và giảm rủi ro token dài hạn bị JavaScript đọc; đánh đổi là flow refresh/retry/reuse detection phức tạp hơn session thuần — xem [`adr/0010-jwt-access-with-rotating-refresh.md`](adr/0010-jwt-access-with-rotating-refresh.md) |
| 66 | Auth TTL theo hai profile: `storefront` access JWT 10 phút, refresh idle 7 ngày, absolute 30 ngày; `admin`/`cms` access JWT 5 phút, refresh idle 8 giờ, absolute 24 giờ | Một TTL chung cho cả ba app; access token dài hạn hơn để giảm refresh | Internal apps có quyền thay đổi catalog, inventory, order và public content nên cần cửa sổ rủi ro ngắn hơn storefront. Absolute lifetime buộc tái xác thực ngay cả khi refresh liên tục; các giá trị là baseline cấu hình, có thể chỉnh sau dựa trên security/UX evidence |
| 67 | Mỗi FE app có registrable domain độc lập; browser chỉ gọi same-origin `/api/*`, hosting/reverse proxy chuyển tiếp sang Backend domain. Refresh cookie là first-party, host-only theo từng app; không gọi trực tiếp cross-site API bằng refresh cookie | Các subdomain dưới một domain gốc; browser gọi trực tiếp API domain bằng credentialed CORS/third-party cookie; OAuth/OIDC redirect flow | Chủ dự án muốn domain độc lập cho từng app. Same-origin proxy giữ độc lập repository/deployment/domain nhưng tránh phụ thuộc third-party cookie có thể bị browser chặn; Backend vẫn là nơi duy nhất xử lý auth và business logic — xem [`adr/0011-independent-app-domains-via-same-origin-api-proxy.md`](adr/0011-independent-app-domains-via-same-origin-api-proxy.md) |
| 68 | Local Backend: NestJS chạy trực tiếp trên host bằng `pnpm start:dev`; Docker Compose chỉ chạy PostgreSQL + Mailpit. Integration test dùng database container riêng; chưa thêm Redis | Chạy toàn bộ NestJS/PostgreSQL trong Compose khi develop; cài PostgreSQL/Mail server trực tiếp trên máy; thêm Redis từ Phase 0 | Host process cho hot reload/debug đơn giản với developer mới làm Backend; Compose giữ dependency stateful lặp lại được; Mailpit cover forgot/reset email mà không gửi thật. Redis chưa có use case bắt buộc; production Dockerfile được làm riêng khi chốt deploy |
| 69 | Backend testing dùng Jest + Supertest + Testcontainers PostgreSQL; integration test không mock database. FE Playwright giữ trách nhiệm journey xuyên FE → API | Vitest để đồng bộ FE; database mock/in-memory; chỉ test HTTP với mocked service | NestJS có paved-road Jest/Supertest; PostgreSQL container thật mới chứng minh migration, constraint, isolation, locking, rollback và chống oversell. Testcontainers tách test database khỏi development state; contract suite kiểm OpenAPI compatibility riêng |
| 70 | Backend logging baseline dùng Pino/`nestjs-pino` với structured JSON, redaction và request/correlation ID; sensitive business actions ghi PostgreSQL audit trail riêng. Monitoring/tracing vendor được hoãn tới trước staging | `console.log`/Nest default logger không chuẩn hóa; thêm OpenTelemetry/Sentry/Datadog ngay Phase 0; trộn audit event vào application log | Pino cung cấp log có cấu trúc và redaction đủ cho scaffold mà chưa khóa vendor. Audit trail cần persistence/queryability riêng cho inventory, order, Admin và CMS; request ID nối proxy → API → log. Hoãn vendor tránh thêm hạ tầng trước khi có môi trường staging |
| 71 | BE request/response contract dùng Nest DTO classes + `class-validator`/`class-transformer` + global `ValidationPipe` + `@nestjs/swagger`; BE không duy trì Zod schema thủ công song song | Zod + community Nest/OpenAPI adapter trong BE; validation thủ công; DTO không có runtime validation | Đây là paved road chính thức của Nest cho validation và OpenAPI code-first, giảm integration phụ cho developer mới làm BE. Global pipe whitelist + từ chối unknown fields; conversion phải tường minh; validation errors map về stable envelope. FE Zod adapters được generate từ OpenAPI theo Decision #64 — xem [`adr/0012-nest-dto-validation-and-openapi.md`](adr/0012-nest-dto-validation-and-openapi.md) |
| 72 | Password được hash bằng Argon2id qua thư viện Node `argon2`; baseline tối thiểu `m=19456 KiB`, `t=2`, `p=1`, sau đó benchmark trên hạ tầng production và tăng cost khi phù hợp. Lưu encoded PHC string với salt ngẫu nhiên do thư viện tạo; rehash-on-login khi tham số cũ. Không mã hóa password, không tạo bcrypt hash mới và chưa thêm pepper ở Phase 0 | bcrypt; scrypt; tự chọn salt/hash primitives; mã hóa password có thể giải ngược; thêm pepper ngay | Argon2id là lựa chọn ưu tiên của OWASP cho hệ thống mới và cân bằng chống side-channel/GPU cracking. PHC string giữ algorithm/parameters/salt để migration lười qua rehash-on-login; chưa dùng pepper vì Phase 0 chưa chốt secrets manager và rotation procedure. Tham số là minimum floor, không thay thế benchmark tài nguyên thật |
| 73 | HTTP API nghiệp vụ dùng NestJS URI versioning dưới `/api/v1`; chỉ mở `/api/v2` cho breaking change. Thay đổi backward-compatible tiếp tục phát hành trong v1. `/health/live` và `/health/ready` là version-neutral. OpenAPI artifact dùng SemVer độc lập với major version trên URI | Custom version header; media-type versioning; mọi thay đổi contract đều tạo URI version mới; API không version | URI version hiện rõ trong request/log/proxy và là cơ chế native đơn giản nhất của NestJS. Tách SemVer artifact khỏi URI tránh nhân bản endpoint cho các thay đổi tương thích, trong khi major URI vẫn tạo migration window rõ khi có breaking change |
| 74 | NestJS dùng `@nestjs/platform-express` (Express adapter) cho phiên bản đầu; ưu tiên Nest platform-neutral abstractions và không truy cập native `Request`/`Response` nếu không cần. Chỉ đánh giá chuyển Fastify sau benchmark có tải đại diện | Fastify adapter ngay từ đầu | Express là adapter mặc định, có hệ sinh thái và tài liệu rộng; Nest file upload/Multer tích hợp sẵn phù hợp CMS/catalog image workflow. Với developer mới làm BE, lợi ích tương thích lớn hơn throughput lý thuyết chưa được chứng minh. Giữ platform-specific code ở infrastructure edge giúp khả năng chuyển adapter về sau |
| 75 | Search catalog MVP dùng PostgreSQL Full-Text Search + `unaccent` + `pg_trgm`; GIN/GiST/B-tree index theo loại truy vấn. Prisma xử lý CRUD, còn migration extension/index và search query đặc thù dùng parameterized raw SQL. Chưa triển khai external search engine | Elasticsearch/OpenSearch/Meilisearch ngay từ đầu; chỉ dùng `ILIKE '%query%'` không index | Một datastore tránh chi phí vận hành và đồng bộ index cho solo developer. PostgreSQL đáp ứng relevance, accent-insensitive và fuzzy/partial matching cho catalog MVP. External engine chỉ được đánh giá khi load test hoặc yêu cầu synonym, merchandising/ranking, analytics hay search scale chứng minh baseline không đủ |
| 76 | Product/CMS media dùng S3-compatible object storage sau interface `ObjectStorage`; PostgreSQL chỉ lưu `Asset` metadata. V1 upload multipart qua Backend và stream sang storage, không buffer toàn file; direct presigned upload được hoãn. Public asset chỉ qua CDN sau publish, draft/preview giữ private | Lưu binary trong PostgreSQL; local disk production; browser direct-presigned upload ngay V1; khóa trực tiếp AWS S3 SDK vào domain logic | Object storage phù hợp file/media và giữ database/Backend stateless. Backend-mediated flow đơn giản hơn cho developer mới, tập trung authorization, content validation và audit; đánh đổi bandwidth chấp nhận ở MVP. Interface S3-compatible giữ quyền chọn vendor, còn presigned flow chỉ thêm khi số liệu file size/bandwidth chứng minh cần |
| 77 | Background work V1 dùng PostgreSQL transactional outbox/durable job table; claim bằng `FOR UPDATE SKIP LOCKED`, delivery at-least-once, idempotent handler, retry/backoff và dead-letter. `@nestjs/schedule` chỉ kích hoạt poll/sweep. Worker chạy cùng Nest application ở V1 nhưng giữ boundary để tách process sau; chưa thêm Redis/BullMQ | Gọi email/external effect đồng bộ trong request; timer in-memory không persistence; Redis + BullMQ ngay; tự nhận là exactly-once | Ghi domain change và job trong cùng PostgreSQL transaction tránh dual-write mất job. PostgreSQL đã là dependency bắt buộc và đủ cho email, reservation expiry, asset cleanup ở tải MVP. At-least-once phản ánh đúng failure model; idempotency xử lý duplicate. Chỉ thêm Redis/BullMQ khi throughput, nhiều worker độc lập hoặc workflow queue nâng cao có bằng chứng |
| 78 | Authorization `admin`/`cms` dùng permission-based guard: `Staff` (entity tách biệt với `Customer`) gán nhiều `Role` (many-to-many); `Role` → tập `Permission` cố định trong code (`resource:action`, ví dụ `order:read`); controller khai báo bằng `RequirePermissions(...)` (AND khi nhiều permission), không check role trực tiếp; `PermissionsGuard` toàn cục, fail-closed khi thiếu decorator, không có role bypass-all ngầm định. `Customer` không tham gia mô hình này | CASL/OPA/Keycloak hoặc permission builder lưu database; role-check trực tiếp trong controller (`if role === 'admin'`); guard fail-open khi thiếu decorator; gán Permission/Role cho cả Customer | Quy mô solo dev/team nội bộ nhỏ (Decision #9) chưa cần policy engine ngoài; permission-based tách "role tên gì" khỏi "được làm gì" nên đổi/tổ hợp role không phải sửa từng controller. Fail-closed vì quên decorator phải lộ ra ngay (mất chức năng) thay vì âm thầm mở quyền. `Customer` dùng ownership check ở domain service thay vì Role vì rule "chỉ xem của chính mình" không diễn đạt được thành permission tĩnh — xem [`adr/0013-permission-based-authorization-admin-cms.md`](adr/0013-permission-based-authorization-admin-cms.md). Danh sách Role thật chưa chốt, xem `open-decisions.md` |
| 79 | Đổi role của `Staff` cũng phải revoke refresh-token family, giống logout/reset password/khóa tài khoản — mở rộng danh sách trigger ở ADR-0010 | Không revoke khi đổi role, để access token cũ tự hết hạn theo TTL rồi refresh lấy role mới từ database | Đổi role thường đi kèm thu hẹp hoặc thu hồi quyền; muốn buộc `Staff` đăng nhập lại ngay, không để session cũ tiếp tục bằng refresh token còn hạn — nhất quán với trigger "khóa tài khoản" đã có — xem [`adr/0010-jwt-access-with-rotating-refresh.md`](adr/0010-jwt-access-with-rotating-refresh.md) |
| 80 | **Thay thế Decision #1 (một phần), #4, #5**: FE không còn 1 Turborepo monorepo. Thay bằng 4 repository riêng biệt — `storefront` (tái sử dụng repo `E-commerce-platforms-FE`/`ecommerce-next` đã có sẵn 36+ PR, không viết lại từ đầu), `admin`, `cms` (repo trống, scaffold sau), và `packages` (chứa `design-tokens`/`tailwind-config`/`ui`/`commerce`/`schemas`/`api-sdk`/`hooks`/`utils`/`eslint-config`/`ts-config`). 3 repo app cài package dùng chung từ repo `packages` qua **git dependency** (`pnpm add github:LuongCongThanh/<packages-repo>#semver:^x.y.z`), không dùng `pnpm-workspace.yaml`/`workspace:*` xuyên repo | Giữ nguyên 1 Turborepo monorepo (Decision #4/#5); publish `packages/*` lên private npm registry/GitHub Packages thay vì git dependency; viết lại `storefront` từ đầu thay vì tái dùng `ecommerce-next` như Decision #1 đã chọn | Chủ dự án xác nhận trực tiếp khi agent đã dựng xong phần monorepo foundation (workspace/turbo/`packages/ts-config`/`packages/eslint-config`) theo Decision #4/#5 gốc — nhận ra `E-commerce-platforms-FE` đã có sẵn lượng code thật đáng kể (36+ PR merged: storefront pages, admin shell, order summary, a11y fix) nên ưu tiên tái dùng thay vì bỏ đi; chấp nhận đánh đổi mất atomic commit xuyên `packages`+app trong 1 lần, cần versioning/tag thật cho repo `packages` và không còn `turbo run` xuyên toàn bộ FE. Phần workspace/turbo/`packages/ts-config`/`packages/eslint-config` đã dựng trong PR đầu của repo `storefront` bị revert khỏi repo đó và chuyển sang sống ở repo `packages` mới |

## Ghi chú

- Đánh số quyết định là liên tục, không tái sử dụng lại số cũ khi một quyết định bị đảo ngược — nếu một quyết định thay đổi, thêm dòng mới, ghi chú "thay thế Decision #N" trong cột Lý do chọn.
- Những quyết định có trade-off lớn hoặc cần lập luận riêng sẽ được tách thêm thành ADR trong [`docs/00-core/adr/`](adr/).
- Khi một quyết định đang được dùng hằng ngày ở code hoặc test, ưu tiên link thẳng tới số quyết định liên quan thay vì bắt người đọc đọc lại toàn bảng từ đầu.
